# Install only the backend(s) you use — all are optional
# pip install text2sg[gemini]   → Gemini (default)
# pip install text2sg[claude]   → Claude
# pip install text2sg[openai]   → OpenAI / GPT
# pip install text2sg[all]      → all backends + dev deps

# For development / running tests:
pytest>=8.0
pandas>=2.0

# Pick one (or more):
# google-genai>=1.0
# anthropic>=0.40
# openai>=1.0
